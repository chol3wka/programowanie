<Window x:Class="Aplikacja.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aplikacja"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox Width="200" Text="{Binding NewItemName}" />
            <ComboBox Width="120" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" />
            <Button Content="dodaj" Width="80" Margin="5,0,0,0" Click="Add_Click" />
        </StackPanel>

        <ComboBox Grid.Row="1" Width="150" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}"/>

        <ListView Grid.Row="2" ItemsSource="{Binding FilteredItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsBought}" />
                        <TextBlock Text="{Binding Name}" Margin="10,0,0,0" Width="150" />
                        <TextBlock Text="{Binding Category}" Margin="10,0,0,0" Opacity="0.6" />
                        <Button Content="Usuń" Margin="10,0,0,0" Click="Delete_Click" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
